# Bug #006: The Random Start

### The Goal
Count to 5 using a loop.

### The Symptoms
The output is a huge random number like "Final count: 32771".

### Hints
1. C does not automatically set variables to 0. It just uses whatever was in that memory spot (garbage values).
2. Always initialize your variables! Try `int counter = 0;`.
 flagship.
