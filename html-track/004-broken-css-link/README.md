# Bug #006: The Vanishing Styles

### The Goal
Make the page blue and the title fancy!

### The Symptoms
The page is plain white and the title is default black. The browser is not loading the CSS file.

### Hints
1. Look at the `<link>` tag in the `<head>`.
2. Check the filename in the `href` attribute against the actual filename in the folder.
