# Bug #003: The Footer Snatcher

### The Goal
The footer should be outside the blue container.

### The Symptoms
The footer is appearing inside the blue border of the container. 

### Hints
1. Count your `<div>` opening and closing tags.
2. Every `<div>` needs a `</div>`.
