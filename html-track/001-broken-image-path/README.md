# Bug #001: The Invisible Logo

### The Goal
Display the company logo on the profile page.

### The Symptoms
Users are reporting that the logo doesn't appear, and they see a broken image icon instead.

### Hints
1. Look at the `src` attribute of the `img` tag.
2. Is the folder name spelled correctly? (Check if it should be `assets` instead of `assts`).
