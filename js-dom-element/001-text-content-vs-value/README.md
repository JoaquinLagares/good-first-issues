# DOM Bug #001: The Ghost Input

### The Goal
Count the number of characters typed in the input field when the button is clicked.

### The Symptoms
The count always stays at "0", no matter what you type.

### Hints
1. For `<input>` elements, what property contains the text typed by the user?
2. Is it `innerText` or `value`?
